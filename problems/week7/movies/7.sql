-- Output a table with two columns, one for the title of each movie and one for the rating of each movie for movies released in 2010, ordered by decreasing rating
SELECT movies.title, ratings.rating FROM movies JOIN ratings ON movies.id = ratings.movie_id WHERE movies.year = 2010 ORDER BY ratings.rating DESC, movies.title;